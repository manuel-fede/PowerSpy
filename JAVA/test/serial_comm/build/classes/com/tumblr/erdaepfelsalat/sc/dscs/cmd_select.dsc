The select command is used to select the port to interact with.
It takes the desired serial port as it's first argument.
This port must be a number.
requirements: The search command must have been invoked.
arguments: <nr>